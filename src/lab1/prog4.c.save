#include <stdio.h>
#include <sys/types.h>
#include <stdlib.h>
#include <unistd.h>

int main()
{
	pid_t p1;
	p1 = fork();

	if(p1 > 0)
	{
		y -= 1;
		printf("Parent process, PID = %d, y = %d\n", getpid(), y);
		exit(0);
	}

	

	return 0;
}
